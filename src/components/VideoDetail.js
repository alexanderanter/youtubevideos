//import react

// create a functional component called VideoDetail with deconstructed prop called video
// write an if statement that if video is not returning trueish, return a div and Loading... inside of it.
// write a return statement underneath with a div wrapper
// inside of the div wrapper another div with classname ui segment
// inside ui segment a h4 with classname ui header and the title of the video
// below the h4, classname ui embed div
// inside ui embed div, an iframe with title video player and the src for the youtube video
// below the ui embed div, place a p tag with the video description.

//export
